# Snyk security configuration

# Severities to check
severity=low=medium=high=critical

# Languages
language=typescript,javascript

# Exclude paths
exclude=tests/**,coverage/**,dist/**,node_modules/**,.next/**
