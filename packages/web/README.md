# @pmp/web

Next.js (App Router) frontend for the PMP study platform.

## Development
- Install deps (from repo root): `npm install`
- Run web: `npm run dev:web`

## Environment
Copy `packages/web/.env.example` to `packages/web/.env.local` and adjust as needed.

