import { Router } from 'express';

const router = Router();

// TODO: Implement practice routes in Task 7
// POST /sessions - Start practice session
// POST /sessions/:id/answers - Submit answer
// POST /sessions/:id/complete - Complete session
// GET /flagged - Get flagged questions
// POST /mock-exams - Start mock exam (High-End+)
// POST /mock-exams/:id/submit - Submit mock exam

export default router;
