import { Router } from 'express';

const router = Router();

// TODO: Implement subscription routes in Task 3
// GET /tiers - Get all subscription tiers
// GET /current - Get current user subscription
// POST /create - Create subscription (initiate PayPal order)
// POST /activate - Activate subscription after PayPal payment
// POST /cancel - Cancel subscription

export default router;
