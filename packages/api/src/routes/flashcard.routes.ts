import { Router } from 'express';

const router = Router();

// TODO: Implement flashcard routes in Task 6
// GET / - Get flashcards with filters
// GET /review - Get cards due for review
// POST /sessions - Start flashcard session
// GET /sessions/:id - Get session details
// POST /sessions/:id/responses - Record response
// POST /sessions/:id/complete - Complete session
// POST /custom - Create custom flashcard (High-End+)

export default router;
